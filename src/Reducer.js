const ActionTypes = {
  REQUEST_POST: 'REQUEST_POST',
  RECEIVE_POST: 'RECEIVE_POST',
  INVALIDATE_POST: 'INVALIDATE_POST'
};

const keys = Object.keys(ActionTypes);

export default function repressReducer (action) {
  switch (action.type) {
    
  }
}
